<% @places.each do |place| %>
  <%= link_to place.address, place_path(place) %><br />
  <%= place.city %>

  <% place.reservations.each do |r|%>
  <%= r.start_date %><%= r.end_date %>
  <%end%><br />
<% end %>